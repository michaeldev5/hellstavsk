import { FC } from "react";
import { TNavigation } from "../types/TCommon";
declare type Props = {
    navigation: TNavigation;
    showHero?: boolean;
};
declare const Nav: FC<Props>;
export default Nav;
