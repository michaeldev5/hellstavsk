import React from 'react';
import styled from 'styled-components';
import Title1 from './Title1';

const Wrapper = styled.div`

`

const ProjectSection = () => (
  <Wrapper>
    <Title1 beforeText="POSLEDNÉ">PROJEKTY</Title1>
  </Wrapper>
);

export default ProjectSection;